<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <link rel="stylesheet" href="/css/main.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <title>Acceso - Ceci Piano Studio</title>
    <style>
        :root {
            --piano-gold: #C9B59C;
            --piano-dark: #C9B59C;
            --piano-beige: #F2E9E1;
        }

        body {
            background: linear-gradient(135deg, var(--piano-beige) 0%, #ffffff 100%);
            font-family: 'Montserrat', sans-serif;
            height: 100vh;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            
        }

        .login-card {
            width: 90%;
            max-width: 400px;
            padding: 2.5rem;
            border-radius: 20px;
            background: white;
            box-shadow: 0 15px 35px rgba(90, 74, 66, 0.1);
            border: 1px solid rgba(201, 181, 156, 0.2);
        }

        .brand-icon {
            font-size: 3rem;
            color: var(--piano-gold);
            margin-bottom: 0.5rem;
        }

        .form-control {
            border-radius: 12px;
            padding: 1rem;
            border: 1px solid #dee2e6;
            background-color: #fcfaf8;
            font-size: 1.1rem;
        }

        .form-control:focus {
            border-color: var(--piano-gold);
            box-shadow: 0 0 0 0.25rem rgba(201, 181, 156, 0.15);
        }

        .btn-piano {
            background-color: var(--piano-dark);
            color: white;
            border-radius: 12px;
            padding: 1rem;
            font-weight: 700;
            letter-spacing: 1px;
            border: none;
            transition: transform 0.2s;
        }

        .btn-piano:active {
            transform: scale(0.98);
        }
    </style>
</head>

<body>
    <div class="login-card text-center">
        <div class="mb-5">
            <div class="brand-icon">
                <i class="bi bi-music-note-beamed"></i>
            </div>
            <h2 class="fw-bold text-dark mb-1">Bienvenido</h2>
            <p class="text-muted small">Ingresa tu usuario para continuar</p>
        </div>

        <form action="/login" method="POST">
            <div class="mb-4 text-start">
                <label for="usuario" class="form-label small fw-bold text-muted ps-1">Nombre de Usuario</label>
                <div class="input-group">
                    <span class="input-group-text bg-transparent border-end-0 text-muted">
                        <i class="bi bi-person"></i>
                    </span>
                    <input type="text" class="form-control border-start-0 ms-0" id="usuario" name="usuario" 
                           placeholder="Ej. maria_piano" required autocomplete="username">
                </div>
            </div>

            <div class="d-grid">
                <button type="submit" class="btn btn-piano btn-lg shadow-sm">
                    ENTRAR AL PANEL
                </button>
            </div>
        </form>

        <div class="mt-4 pt-2">
            <p class="text-muted" style="font-size: 0.75rem;">
                ¿Tienes problemas para acceder? <br>
                <a href="#" class="text-decoration-none" style="color: var(--piano-gold);">Contacta a soporte</a>
            </p>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/login.js"></script>
</body>

</html>