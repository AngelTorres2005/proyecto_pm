<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <title>Mis Tareas - Ceci Piano Studio</title>
    <style>
        :root {
            --piano-gold: #C9B59C;
            --piano-dark: #5a4a42;
            --piano-bg: #faf8f7;
            --piano-beige: #F2E9E1;
        }

        body { background-color: var(--piano-bg); font-family: 'Segoe UI', system-ui, sans-serif; }


        #notificacionesBanner {
            border: none;
            border-radius: 12px;
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: white;
            box-shadow: 0 4px 15px rgba(37, 117, 252, 0.2);
        }

        .tareas-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.03);
            border: 1px solid rgba(0,0,0,0.05);
            overflow: hidden;
        }

        .table thead th {
            background-color: var(--piano-beige);
            color: var(--piano-dark);
            text-transform: uppercase;
            font-size: 0.75rem;
            letter-spacing: 1px;
            padding: 1.2rem;
            border: none;
        }

        @media (max-width: 768px) {
            h2 { font-size: 1.5rem; font-weight: 800; text-align: center; }
            .text-muted.small { text-align: center; display: block; margin-bottom: 1.5rem; }

            .table thead { display: none; } 
            
            .table, .table tbody, .table tr, .table td { display: block; width: 100%; }

            .table tr {
                margin-bottom: 15px;
                border: 1px solid var(--piano-beige);
                border-radius: 15px;
                padding: 12px;
                background: #fff;
            }

            .table td {
                display: grid;
                grid-template-columns: 100px 1fr; 
                text-align: left;
                padding: 10px 15px !important;
                border: none !important;
                align-items: center;
            }


            .table td::before {
                content: attr(data-label);
                font-weight: 800;
                text-transform: uppercase;
                font-size: 0.65rem;
                color: var(--piano-gold);
            }
            
            #btnActivarNotificaciones { width: 100%; }
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-light bg-white shadow-sm sticky-top py-2">
        <div class="container">
            <a class="navbar-brand fw-bold d-flex align-items-center" href="/">
                <i class="bi bi-music-note-list me-2" style="color: var(--piano-gold);"></i>
                <span class="d-none d-sm-inline">CECI PIANO STUDIO</span>
                <span class="d-inline d-sm-none">MIS TAREAS</span>
            </a>
            <a class="btn btn-outline-dark btn-sm rounded-pill px-3 fw-bold" href="/">
                Salir <i class="bi bi-box-arrow-right ms-1"></i>
            </a>
        </div>
    </nav>

    <div class="container mt-4">
        <div id="notificacionesBanner" class="alert d-flex align-items-center justify-content-between flex-wrap gap-3 p-3 mb-4 shadow-sm" role="alert" style="display: none !important;">
            <div class="d-flex align-items-center">
                <i class="bi bi-bell-fill fs-4 me-3"></i>
                <div>
                    <strong class="d-block">¡No pierdas ni una práctica!</strong>
                    <small id="notificacionesTexto">Activa los avisos de nuevas tareas.</small>
                </div>
            </div>
            <button type="button" id="btnActivarNotificaciones" class="btn btn-light btn-sm fw-bold rounded-pill px-4 shadow-sm">
                Activar
            </button>
        </div>

        <div class="row mb-3">
            <div class="col-12">
                <h2 class="text-dark mb-1">Tareas de Hoy</h2>
                <p class="text-muted small"><i class="bi bi-calendar3 me-1"></i> Revisa el progreso de tus hijos.</p>
            </div>
        </div>

        <div class="tareas-container">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead>
                        <tr>
                            <th class="py-3 ps-4 text-start">Alumno</th>
                            <th class="py-3 ps-4 text-start">Tarea Asignada</th>
                            <th class="py-3 ps-4 text-start">Fecha Límite</th>
                        </tr>
                    </thead>
                    <tbody id="tablaAlumno">
                        </tbody>
                </table>
            </div>
        </div>
        
        <div class="mt-5 text-center pb-4">
            <p class="text-muted" style="font-size: 0.7rem;">&copy; 2026 Ceci Piano Studio</p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>window.PUBLIC_VAPID_KEY = "<%= publicVapidKey %>";</script>
    <script src="/js/usuario/inicioUsuarios.js"></script>
</body>
</html>